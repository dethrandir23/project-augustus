{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "mkdir -p build && cd build && cmake -G Ninja .. && ninja",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"]
    },
    {
      "label": "clean-build",
      "type": "shell",
      "command": "rm -rf build && mkdir build && cd build && cmake -G Ninja .. && ninja",
      "problemMatcher": ["$gcc"]
    }
  ]
}